# 변경 이력

버전 규칙: `major.minor.patch`
- **major**: 주요 기능 변화
- **minor**: 마이너 기능 변화
- **patch**: 버그 수정

## 2.12.0

- **마이너 기능(서버 정책 변경 대응)**: 게이트웨이(qdrant_mcp v2.0.0)가 개인/공용/제안서
  저장소를 완전히 배타적으로 분리하면서, 개인 키(`MCP_URL`)로 공용/제안서 저장소의
  삭제·검색을 호출하던 기존 코드가 전부 "접근 권한이 없습니다" 에러로 실패하게 됨 - 이를
  저장소별 올바른 키로 라우팅하도록 수정.
  - `_call_delete_tool()`/`_raw_find()`가 이제 호출부에서 `mcp_url`을 명시적으로 받도록
    변경(예전엔 항상 `MCP_URL` 하드코딩).
  - `delete_by_source()`/`search_qdrant()`(공용) → `MCP_URL_SHARED`,
    `delete_proposal_by_source()`(제안서) → `MCP_URL_PROPOSAL`,
    `delete_mine_by_source()`/`search_my_qdrant()`(개인) → `MCP_URL`(변경 없음).
  - 해당 URL이 config.json에 설정 안 돼 있으면(예: `mcp_url_proposal` 비어있음) 에러
    대신 안내 메시지만 출력하고 건너뜀(기존 `resolve_store_targets()`와 동일한 패턴).
  - 실제 서버로 저장→검색→삭제 전체 흐름을 저장소별 전용 키로 검증 완료.

## 2.11.0

- **마이너 기능**: 세 번째 저장 대상 "제안서 자료 저장소"(게이트웨이의 두 번째 공용
  컬렉션 `proposal_data`) 추가(명시적 사용자 요청: "벡터 공용 저장소를 한개 더 만들고
  싶어 ... 벡터저장 윈도우 프로그램에 체크 박스 추가해").
  - 등록 화면에 "제안서 자료 저장소에 등록" 체크박스 추가(개인/공용과 동일하게 기본
    꺼짐, 여러 개 동시 체크 가능). `resolve_store_targets()`가 `personal`/`shared`에
    이어 `proposal` 인자를 받도록 확장, 저장 대상 툴 매핑을 `_STORE_TOOL_INFO` dict
    하나로 일반화(전과 달리 향후 저장소가 또 늘어도 dict 항목 하나만 추가하면 됨).
  - "설정" 화면에 "제안서 자료 저장소 URL (mcp_url_proposal)" 입력란 추가(명시적 사용자
    요청: "윈도우 등록 프로그램에 저장소 편집 기능도 추가해") - `save_mcp_urls()`가
    세 번째 URL도 함께 저장.
    상단 키 상태 표시에도 "제안서키" 항목 추가(개인/공용키와 동일한 패턴, 미설정이면
    "미설정"으로만 표시).
  - `delete_proposal_by_source()` 추가, "파일 단위 삭제"(`delete_from_all`)가 이제
    공용/개인/제안서 자료 세 곳 모두에서 삭제 시도(등록 안 됐던 곳은 0건으로 조용히
    넘어감) - 확인 대화상자 문구도 세 곳 모두 언급하도록 수정.
  - 실제 로컬 게이트웨이로 저장→검색→삭제 전체 흐름 검증 완료.

## 2.10.0

- **마이너 기능**: 프로그램 시작 시 개인/공용 저장소 키가 유효한지 자동으로 한 번 확인해서
  상단에 각각 따로 표시(명시적 사용자 요청: "클라이언트가 인증되지 않은 키로 접속할 때 응답
  API 만들고", "프로그램/GUI 시작 시 한 번", "개인키, 공용키 구분해서 메시지 알려줘"). 지금까지는
  키가 잘못됐어도 실제 등록/검색을 시도하기 전까진 알 방법이 없었음.
  - 서버(`qdrant_mcp`) 쪽은 변경 없음 — 새 엔드포인트를 추가하지 않고, `KeyAuthMiddleware`가
    이미 모든 요청에 대해 돌려주던 401 JSON(`{"error": "Unauthorized", "reason": ...}`)을
    `register.check_key_status()`가 가벼운 HTTP GET 하나로 트리거해서 그대로 파싱하는 방식.
    실제 MCP 초기화 핸드셰이크(`ClientSession.initialize()`)는 필요 없음.
  - `register.check_key_status(mcp_url)` — `{"ok": bool, "reason": str|None}` 반환. URL
    미설정(`REPLACE_ME` 기본값)/네트워크 실패/401 세 가지를 구분해서 사유를 알려줌.
  - GUI 상단에 "개인키: ✓ 정상" / "공용키: ✗ <사유>" 형태로 색상(초록/빨강) 구분 표시.
    공용 저장소 URL은 필수가 아니라(비워둘 수 있음) 비어 있으면 "공용키: 미설정"으로만
    표시하고 에러 취급하지 않음. 네트워크 요청은 블로킹이라 백그라운드 스레드에서 실행 —
    UI 갱신은 `self.root.after(0, ...)`로 메인 스레드에 위임.
  - 로컬 임베디드 게이트웨이로 유효 키/잘못된 키/미설정 URL 세 가지 케이스 모두 실제 서버
    응답을 받아 정확히 구분되는 것을 확인함(운영 서버/실제 키는 건드리지 않고 검증).

## 2.9.1

- **버그 수정**: 게이트웨이(`qdrant_mcp`)가 `qdrant_delete_mine`에 `qdrant_delete`와 동일한 `source`
  서버 사이드 정확 필터를 지원하도록 배포되어, 2.9.0에서 썼던 "의미 기반 검색으로 후보를 찾아
  point_id를 모아 지우는" 최선 노력 우회를 제거함. `delete_mine_by_source()`가 이제
  `qdrant_delete_mine`을 `source`만 넘겨 직접 호출 — 검색 limit(10)과 무관하게 항상 정확히
  전부 삭제됨. "파일 단위 삭제" 확인창의 "개인 저장소는 일부가 남을 수 있음" 경고 문구도 더
  이상 사실이 아니라 제거함. `_call_qdrant_delete`를 툴 이름을 받는 `_call_delete_tool()`로
  일반화해서 공유/개인 삭제가 파싱 로직을 공유하도록 정리.
  실제 로컬 게이트웨이로 11청크(검색 limit=10 초과) 등록 후 `delete_mine_by_source()` 한 번
  호출로 정확히 11개 전부 삭제되는 것을 확인.

## 2.9.0

- **버그 수정 겸 마이너 기능**: "파일 단위 삭제"가 **개인 저장소 항목은 사실상 항상 삭제 실패**하던
  문제 발견 및 수정. 원인은 `delete_by_source()`가 팀 공유 전용 `qdrant_delete`(source 정확
  필터 지원) 툴만 호출했는데, 개인 저장소의 `qdrant_delete_mine`은 source 필터를 지원하지 않고
  정확한 point_id 목록만 받기 때문에 항상 0개 삭제로 조용히 실패했음("개인 저장소에 등록"이
  기본값이라 실질적으로 거의 항상 해당).
  - `delete_mine_by_source()` 추가: 파일명으로 의미 기반 검색을 돌려 source가 정확히 일치하는
    항목들의 point_id를 모아 `qdrant_delete_mine`으로 삭제 (게이트웨이에 source 필터가 없어
    검색 기반 최선 삭제 - 청크가 아주 많은 파일은 일부가 남을 수 있음, 확인창에 안내 문구 추가)
  - `delete_from_all()` 추가: 팀 공유 + 개인 저장소 양쪽에서 삭제. "파일 단위 삭제" GUI가
    `delete_by_source()` 대신 이걸 사용하도록 변경
- **버그 수정**: "텍스트 붙여넣기로 등록" 후 "진행 상태/오류" 로그가 영구히 멈추던 문제 수정
  (`register_pasted_text()`가 진행률 콜백을 감싸지 않아 GUI 큐에 dict 대신 정수가 들어가
  `poll_queue()`가 예외로 죽으면서 재예약이 끊기던 것). `register_targets`와 동일한 방식으로
  `_unit_progress` 래퍼 추가. 겸사겸사 `poll_queue()` 전체를 try/finally로 감싸서 앞으로
  비슷한 예외가 나도 로그 폴링 자체는 항상 계속되도록 방어 코드 추가.

## 2.8.1

- **버그 수정**: "텍스트 붙여넣기로 등록"을 쓰면 그 이후로 "진행 상태/오류" 로그가 영구히 멈추는
  문제 수정 (앱을 재시작하기 전까지 어떤 작업을 해도 로그가 안 뜸).
  - 근본 원인: `register_pasted_text()`가 파일 등록(`register_targets`)과 달리 진행률
    콜백을 감싸지 않고 그대로 넘겨서, `_report()`가 `progress_callback(unit_index, unit_total,
    unit_label)`처럼 3개 인자로 직접 호출함. 이 콜백이 GUI의 `progress_queue.put`이다 보니
    `Queue.put(item, block, timeout)`로 해석되어 dict 대신 정수(unit_index)가 큐에 들어갔고,
    이걸 꺼내 쓰는 `update_progress_display()`가 `TypeError`로 죽으면서 `poll_queue()`의
    100ms 재예약(`root.after`)이 끊겨버렸음. `register_targets`가 쓰는 것과 동일한 방식으로
    `register_pasted_text()`에도 `_unit_progress` 래퍼를 추가해 올바른 dict 형태로 전달하도록 수정.
  - 방어 코드: `poll_queue()` 전체를 try/finally로 감싸서, 앞으로 비슷한 예외가 나더라도
    재예약(`root.after(100, self.poll_queue)`)만은 항상 일어나도록 해 로그가 영구히 멈추는
    일 자체를 막음.

## 2.8.0

- **마이너 기능**: "설정..." 창에 위키 로그인 계정/비밀번호(`wiki_username`/`wiki_password`) 편집
  필드 추가 - MCP URL과 마찬가지로 config.json 파일을 직접 열지 않고 GUI에서 바로 등록/수정
  가능. 비밀번호 입력칸은 기본적으로 `*`로 가려지며 "표시" 체크박스로 평문 확인 가능
  (`wiki_upload.save_wiki_credentials()` 추가).

## 2.7.0

- **마이너 기능**: 상단에 "설정..." 버튼 추가 — 개인/공용 저장소 MCP 서버 URL(`mcp_url`/
  `mcp_url_shared`)을 `config.json` 파일을 직접 열어 편집하지 않고 GUI에서 등록/수정할 수
  있게 함. 저장하면 파일에도 반영되고, 앱을 재시작하지 않아도 `register.MCP_URL`/
  `MCP_URL_SHARED` 값이 즉시 갱신되어 바로 다음 등록부터 새 URL을 사용함
  (`register.save_mcp_urls()` 추가).

## 2.6.1

- **버그 수정**: "키워드로 검색해서 삭제"가 실제로는 의미(벡터 유사도) 기반 검색이라 관련도
  임계값이 없어, 검색어와 전혀 무관한 내용도 "그나마 가까운 결과"로 항상 나오던 문제 완화.
  `register._keyword_filter()`를 추가해서 `search_qdrant()`/`search_my_qdrant()` 결과 중
  검색어의 각 단어가 실제로 본문 또는 제목에 (대소문자 무시) 포함된 것만 남기도록 함
  ("네이버시스템"으로 검색했는데 그 단어가 전혀 없는 법인카드 규정 등이 나오던 사례로 발견).

## 2.6.0

- **마이너 기능**: 화면 맨 위 문서 등록 영역이 다른 섹션(위키 문서 등록 등)과 달리 제목/설명이
  없는 `LabelFrame` 없는 구조라 "이게 무슨 기능인지" 안 드러나던 문제 개선. "Qdrant 문서 등록"
  이라는 제목의 `LabelFrame`으로 감싸고, "문서를 Qdrant 벡터DB에 등록해서 AI가 내용을 검색하고
  답변에 활용할 수 있게 합니다"라는 설명 문구를 추가. 드래그앤드롭 안내 문구도 위키 섹션과
  동일하게 "Qdrant에 등록"을 명시하도록 수정.
  - 개인/공용 저장소 체크박스는 파일/폴더/드래그앤드롭/텍스트 붙여넣기 등록 전부에 적용되는
    가장 중요한 설정이라, 이 섹션 맨 위(설명·드래그앤드롭 영역보다도 위)로 배치해 등록 전에
    먼저 확인하게 함.

## 2.5.1

- **마이너 기능**: 상태 표시줄("대기 중"/"처리 중..." 등)이 정적 텍스트라 작업이 실제로 진행 중인지
  멈춘 건지 한눈에 안 들어오던 것을, 회전 스피너(◐◓◑◒, 150ms 간격)와 색상 변화(파란색=작업 중,
  회색=대기)로 개선. 등록/삭제/검색/위키 업로드 등 `self.busy`로 관리되는 모든 작업에 공통 적용
  (`App.set_status()` 추가, 각 작업 시작 지점에서 `status_label.config()` 대신 이걸 호출).
  겸사겸사 "분류선택" 버튼(위키 분류 조회)이 `self.busy`를 아예 설정하지 않아 다른 작업과 동시
  실행을 막지 못하던 기존 버그도 함께 수정.

## 2.5.0

- **마이너 기능**: 개인 저장소에 더해 별도 MCP 서버(URL)를 쓰는 **공용 저장소**를 추가.
  `config.json`에 `mcp_url_shared` 키를 추가해 공용 저장소 접속 정보를 개인(`mcp_url`)과
  분리 관리. GUI에는 기존 "개인 저장소에 등록" 체크박스 옆에 "공용 저장소에 등록" 체크박스를
  새로 추가함(기본값: 꺼짐 - 팀 전체가 보게 되는 저장소라 실수로 올리는 일이 없도록).
  - 기존에는 "개인/팀 공유" 중 하나만 고르는 단일 체크박스(상호 배타적)였는데, 이제는 개인/공용이
    서로 독립적인 체크박스로 바뀌어 **둘 다 체크하면 같은 내용을 두 저장소 모두에 저장**함.
    파일 등록, 텍스트 붙여넣기 등록 모두 동일하게 적용(`register.main`/`register_pasted_text`가
    `store_tool` 문자열 대신 `personal`/`shared` 두 개의 bool을 받도록 변경).
  - `mcp_url_shared`가 비어 있는데 공용 체크박스를 켠 경우, 저장을 시도하지 않고 안내만 출력
    (개인 등록까지 막지는 않음).

## 2.4.1

- **버그 수정**: "텍스트 붙여넣기로 등록"에서 제목을 비운 채 등록하면 "(직접 입력) 붙여넣은 시각"
  형식으로 제목이 자동 생성되는데, 나중에 검색·삭제할 때 이게 무슨 내용인지 알아보기 어려운
  문제가 있었음(실제로 제목 없이 등록된 항목을 나중에 못 찾는 사례 발생). 제목이 비어 있는
  상태로 "텍스트 등록"을 누르면 확인 팝업을 띄워 실수로 빈 제목으로 등록하는 것을 막음.

## 2.4.0

- **마이너 기능**: 이미지 캡션을 한글로도 번역해서 함께 저장. moondream(비전 모델) 자체는 한글
  생성 품질이 낮아 캡션은 계속 영어로 생성하되, 그 영어 캡션을 로컬 Ollama의 일반 텍스트 LLM
  (`translate_model`, 기본값 `gemma3:4b`)으로 한글 번역해서 **영어+한글 둘 다** 저장함
  (`register.translate_to_korean()`). 검색용 텍스트에도 "설명(한글): ..."로 포함되어 한글
  키워드로도 이미지가 검색됨. `caption_ko` 메타데이터 필드로도 별도 저장.
  - `config.json`의 `translate_caption`(기본값 `y`)으로 끄고 켤 수 있음 - 이미지 한 장당
    Ollama 호출이 캡션+번역 2번이 되어 등록 속도가 느려지므로, 속도가 중요하면 `n`으로 끌 것

## 2.3.5

- **버그 수정**: 해상도가 낮은 이미지(특히 작은 텍스트 라벨이 많은 인포그래픽류)에서 OCR 결과가
  거의 다 깨진 글자로 나오던 문제 수정. 원인은 원본 해상도가 너무 작아서(예: 670×354) 글자가
  몇 픽셀 단위로 뭉개져 있던 것 - Tesseract 한국어 언어팩(`tesseract-lang`) 문제와는 별개의
  원인이었음. `ocr_image()`에 `OCR_MIN_SIDE`(1600px) 기준을 추가해서, 이보다 작은 이미지는
  OCR 전에 업스케일(최대 변 기준)하도록 함 - 테스트 이미지 기준 거의 읽을 수 없던 결과가 실제
  라벨과 일치하는 한글로 크게 개선됨.

## 2.3.4

- **버그 수정**: Qdrant에 등록/삭제할 때 쓰는 `source`/`title` 문자열이 등록한 OS에 따라 유니코드
  정규화 형태가 달라지던 문제 수정. macOS(APFS/HFS+)는 한글 등을 NFD(분해형)로 반환하는 경우가
  많은데, Windows나 사람이 직접 타이핑하는 경로는 보통 NFC(조합형)라서, 같은 파일인데도 바이트가
  달라 `qdrant_view_image`나 "파일 단위 삭제"의 source 정확 일치 조회가 조용히 실패하던 문제였음
  (겉보기엔 완전히 같은 경로 문자열인데 "해당 항목을 찾을 수 없습니다"가 뜸).
  `register.py`에 `normalize_source()`를 추가해 Qdrant에 쓰거나 조회하는 source/title을 항상
  NFC로 통일함. 윈도우/맥 어느 쪽에서 등록하든 같은 파일이면 항상 같은 source 문자열이 되도록 함.
  (주의: 이미 등록되어 있는 기존 데이터의 정규화 형태까지 소급해서 고치진 못함 - 새로 등록/삭제하는
  것부터 적용됨)

## 2.3.3

- **버그 수정**: Qdrant 저장(`_store_in_batches`)이 배치 저장 호출 실패 시 `[오류]` 로그만 남기고
  넘어가면서도, 반환값(=로컬 덤프 파일 `extracted_text/*.txt`에 그대로 쓰이는 값)에는 실패한 항목까지
  포함시켜 마치 전부 성공한 것처럼 보이던 문제 수정 (141개 이미지 중 특정 페이지만 실제로는
  등록 안 됐는데 덤프에는 141개 다 있던 사례로 발견).
  - 배치 저장 실패 시 같은 배치를 최대 3번(`STORE_MAX_RETRIES`) 재시도
  - 그래도 실패하면 그 배치만 항목별로 나눠 각각 재시도 (배치 안 항목 하나 때문에 전체가
    죽는 것 방지)
  - 재시도 후에도 실패한 항목은 반환값/덤프에서 제외하고, "N개 중 M개 실패"로 명확히 로그 남김

## 2.3.2

- **버그 수정**: "URL 목록으로 위키 등록"(뉴스 스크랩) 박스에 macOS Cmd+V 대응용 "붙여넣기" 버튼이
  빠져 있어, 1.1.2에서 다른 입력창(텍스트 붙여넣기 등록)에 적용한 것과 같은 macOS Cmd+V 미동작
  환경에서 이 박스만 붙여넣을 방법이 없던 문제 수정. 안내 라벨 옆에 "붙여넣기" 버튼 추가.

## 2.3.1

- **버그 수정**: "텍스트 붙여넣기로 등록"/"URL 목록으로 위키 등록"의 입력 박스(`tk.Text`)에 테두리
  설정이 아예 없어서 일부 macOS Tk 빌드에서 배경과 구분이 안 되고 경계가 안 보이던 문제 수정.
  `relief="solid", bd=1`을 명시해서 다른 입력창들과 동일하게 테두리가 보이도록 함.

## 2.3.0

- **마이너 기능**: "위키 문서 등록" 섹션의 "폴더 선택..." 버튼 옆에 **"첨부파일 업로드" 체크박스** 추가.
  2.2.0에서 항상 켜져 있던 원본 파일 첨부 업로드를 이제 이 체크박스로 켜고 끌 수 있음. 기본값은
  **체크 해제(끔)** - 위키 서버 용량/속도 부담 때문에 텍스트만 등록하는 게 기본, 필요할 때만 체크해서
  첨부까지 함께 업로드 (`wiki_upload.upload_paths_to_wiki()`에 `upload_attachment` 인자 추가).

## 2.2.0

- **마이너 기능**: "위키 문서 등록"(파일/폴더 선택·드래그앤드롭)에서 텍스트 등록에 이어 **원본 파일 자체도
  첨부파일로 함께 업로드**하도록 함 (`wiki_upload.upload_file_attachment()`, `mwclient.Site.upload()` 사용).
  업로드 성공 시 문서 본문 끝에 `== 첨부파일 ==` 절로 `[[Media:...]]` 링크를 자동으로 추가.
  위키 서버 설정(허용 확장자/업로드 용량 제한 등)에 따라 첨부만 거부될 수 있는데, 그 경우 예외 없이
  안내만 찍고 본문 텍스트 등록은 그대로 진행 (기존 항목 재등록 시 덮어쓰도록 `ignore=True` 사용).
  "URL 목록으로 위키 등록"(뉴스 스크랩)은 로컬 원본 파일이 없으므로 대상에서 제외.

## 2.1.0

- **마이너 기능**: "위키 문서 등록" 섹션 아래에 **"URL 목록으로 위키 등록"** 추가. 제목/URL을 한 줄씩
  번갈아 텍스트박스에 붙여넣으면 각 URL을 스크랩해서 별도의 위키 페이지로 업로드. 예전
  `site_to_mediawikiupload.py` + `sites.txt` 파일 조합을 대체 - 파일 편집 없이 GUI에서 바로 붙여넣기
  (`wiki_upload.upload_sites_to_wiki()`, 형식/제목 정제/카테고리 태그는 원본 스크립트와 동일하게 유지)
  - 원본 스크립트가 쓰던 `truststore.inject_into_ssl()`은 일부러 빼고 순수 `requests`만 사용 - 그
    함수는 전역으로 `ssl.SSLContext`를 패치해서, 1.1.0에서 고쳤던 "Python 3.14 + truststore 조합
    SSL 무한 재귀" 버그를 다시 일으킬 위험이 있었음

## 2.0.0

- **주요 기능**: 하나의 GUI(`gui.py`)에서 Qdrant 등록과 **위키(MediaWiki) 문서 등록을 함께 사용**할 수 있도록 통합.
  - 새 모듈 `wiki_upload.py`: 문서를 MediaWiki 페이지로 자동 업로드. 예전 `Proposal_to_wikiUpload.py`가
    win32com으로 Excel/한글/PowerPoint를 직접 열어 PDF로 변환한 뒤 텍스트를 뽑던 것과 달리, 이미 있는
    `register.py`의 텍스트 추출기(PDF/PPTX/PPT/XLSX/HWPX/HWP/DOCX/DOC/HTML/TXT/MD, 전부 순수 Python)를
    재사용 - Office 설치나 Windows 종속성 없이 macOS/Linux에서도 동일하게 동작
  - GUI에 "위키 문서 등록" 섹션 추가: 전용 드래그 앤 드롭 영역, 파일/폴더 선택, "분류선택"(위키에 있는
    분류 중 골라서 적용)/"분류텍스트 입력"(새 분류명 직접 입력) 버튼. 기존 진행률 표시줄·로그창을 그대로 공유
  - 접속 정보(`key.txt`)를 `config.json`의 `wiki_site_url`/`wiki_path`/`wiki_username`/`wiki_password`/
    `wiki_category`로 이전 - 다른 설정과 마찬가지로 한 곳에서 관리하고 git에는 올라가지 않음
- **마이너 기능**: Qdrant 등록 지원 형식에 HTML(`.html`/`.htm`) 추가. `lxml.html`로 `<script>`/`<style>`을
  제거하고 블록 요소(문단/제목/리스트 등) 경계에 줄바꿈을 넣어 읽기 좋은 텍스트로 추출 (`register.read_html`).

## 1.1.3

- **버그 수정**: macOS `.app` 번들로 빌드하면 `config.json`/`incoming`/`extracted_images`/`extracted_text`가
  `qdrant_register_gui.app/Contents/MacOS/` 안쪽 깊숙이 생성되어 Finder에서 접근하기 불편했던 문제 수정.
  이제 macOS `.app` 번들에서 실행 중이면 번들이 놓인(사용자 눈에 보이는) 폴더를 기준으로 잡아, Windows exe와
  동일하게 "실행 파일과 같은 경로"에 생성/조회되도록 함 (`register.py`의 `SCRIPT_DIR` 계산 수정).

## 1.1.2

- **버그 수정**: 1.1.1의 Command-v 키 바인딩 수정만으로는 일부 macOS 환경에서 여전히 붙여넣기가 안 되는
  것으로 확인됨(Tk 기본 메뉴가 Cmd+V를 먼저 가로채는 것으로 추정, 정확한 원인 미확정). 키보드 단축키에
  의존하지 않도록 "텍스트 붙여넣기로 등록" 섹션의 제목/본문 입력창 옆에 **붙여넣기 버튼**을 추가해서
  클립보드 내용을 직접 삽입하도록 함.

## 1.1.1

- **버그 수정**: macOS에서 Entry/Text 위젯(텍스트 붙여넣기 제목/본문, 검색창 등)에 Command-V(붙여넣기)/Command-C(복사)/Command-X(잘라내기)/Command-A(전체선택)가
  동작하지 않던 문제 수정. 일부 Tcl/Tk 빌드는 macOS 기본 단축키 바인딩이 빠져있어 명시적으로 바인딩 추가.

## 1.1.0

- **마이너 기능**: Windows/macOS/Linux 크로스플랫폼 지원.
  - Tesseract, LibreOffice 실행 파일을 PATH 우선 탐색 후 OS별(Windows/macOS Homebrew/Linux)
    흔한 설치 경로로 자동 탐색 (`find_tesseract`, `find_soffice`)
  - GUI 폰트(한글 표시용, 로그/검색결과 고정폭용)를 OS별로 자동 선택
  - 코드 자체는 이제 플랫폼 무관하게 동작하지만, macOS/Linux용 실행파일은
    해당 OS에서 직접 PyInstaller로 빌드해야 함 (크로스 컴파일 불가 — `python gui.py`로는
    바로 실행 가능)

## 1.0.1

- **버그 수정**: 청크 분할(`chunk_text`)이 문자 수로만 잘라서 엑셀 행이나 문장이 청크 경계
  중간에서 잘리던 문제 수정. 이제 줄(line) 단위를 지켜서 나누고, 한 줄 자체가 청크 크기보다
  긴 경우에만 예외적으로 그 줄만 문자 단위로 쪼갠다. (네이버시스템_제안서_리스트.xlsx 등록 후
  검색 결과가 잘려 보인다는 리포트로 발견)

## 1.0.0

최초 릴리스 기준선. 이 시점까지 만들어진 기능:

- PDF/PPTX/PPT/XLSX/HWPX/HWP/DOCX/DOC/TXT/MD/이미지 파일 등록 (텍스트+이미지 함께 추출)
- 이미지 OCR(Tesseract) + 캡션(로컬 Ollama moondream) 자동 생성
- 페이지/슬라이드/시트당 가장 큰 이미지 3장으로 제한 (성능)
- OCR/캡션 저장용 이미지 다운스케일
- Qdrant에 등록된 내용을 로컬 텍스트로 덤프 (`extracted_text/`)
- 드래그 앤 드롭 + 파일/폴더 선택 + 텍스트 붙여넣기 등록
- 개인 저장소 / 팀 공유 저장소 선택
- 파일 단위 삭제, 키워드 검색 후 선택 삭제 (개인/팀 각각)
- 실시간 진행률(파일 단위 + 페이지/슬라이드/이미지 단위)
- 저장 속도 개선: 서버 배치 저장 API 우선 사용, 없으면 동시 저장으로 자동 폴백
- 화면 상단 버전 표시
