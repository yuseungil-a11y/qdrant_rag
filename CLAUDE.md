# 작업 지침

- 중요하지 않은(하나만 골라도 무방한, 되돌리기 쉬운) 선택은 묻지 말고 자동으로 판단해서 진행할 것. 사용자에게 확인을 구하는 건 실제로 방향이 크게 갈리거나 되돌리기 어려운 결정일 때만.
- **버전 규칙 (major.minor.patch, 예: 1.0.0)**: `gui.py`의 `APP_VERSION`을 이 규칙으로 관리한다.
  - **major**: 주요 기능 변화 (새로운 큰 기능, 기존 동작을 크게 바꾸는 변경)
  - **minor**: 마이너 기능 변화 (작은 기능 추가, 개선)
  - **patch**: 버그 수정
  - GUI를 재빌드할 만한 코드 변경을 했으면, 위 기준에 맞게 `APP_VERSION`을 올리고 `CHANGELOG.md`에 항목을 추가할 것. 사소한 리팩터링/문서 수정만 있었다면 버전을 올리지 않아도 됨 — 애매하면 patch로 처리.
  - `APP_VERSION`을 올릴 때마다(커밋 여부와 무관하게, 버전이 새로 생기는 시점) PMS 이슈 http://pms.utinfo.co.kr:1177/redmine/issues/23718 에 그 버전의 수정 내용을 댓글로 자동 등록할 것(`mcp__PMS_redmine__issues_update`의 `id`/`notes`만 사용, 다른 필드는 건드리지 않음) — 사용자에게 묻지 않고 매번 수행. 여러 버전을 한 번에 올렸으면 버전별로 구분해서 한 댓글에 정리해도 됨.
  - **댓글 서식 주의**: 이 PMS의 댓글(journal notes)란은 Textile/Markdown을 해석하지 않고 `\n`/`\n\n`도 화면에 줄바꿈으로 반영되지 않는다(일반 텍스트로 그대로 붙여서 렌더링됨) — 실측 확인됨. 줄바꿈이 실제로 보이게 하려면 각 줄 끝에 **`<br><br>`**(두 개 연속)를 넣을 것. 버전 하나당 "vX.Y.Z - 한 줄 요약." 형태로 아주 짧게 쓰고, 항목 사이는 `<br><br>`로 구분한다(위 예시 그대로 따라 하면 됨 - 2026-09-04에 이 형식으로 정상 렌더링 확인함).
- **실행 파일 이름은 `utinfo_vdr.exe`** (`qdrant_register_gui.spec`의 `name=` 값, 예전엔 `qdrant_register_gui.exe`였음 - 2026-09-05 사용자 요청으로 변경). GUI 창 제목("Qdrant 문서 등록 vX.Y.Z")이나 소스 파일명(`gui.py` 등)은 그대로 유지, 오직 빌드 산출물 exe 이름만 바뀐 것.
- **자기업데이트용 게시(`releases/` 폴더)는 사용자가 명시적으로 "배포해줘"/"게시해줘"라고 할 때만 커밋/푸시할 것.** 평소 버전 올리고 빌드하는 것은 지금처럼 자유롭게 하되(그 결과물은 gitignore된 `dist/`에만 둠), `releases/windows/utinfo_vdr.exe`(~79MB)는 버전 올릴 때마다 자동으로 갱신하지 않는다 — 매번 커밋하면 레포가 순식간에 수백MB~GB로 불어나기 때문(사용자 확인: "사용자가 명시적으로 '배포/게시해줘'라고 할 때만").
  - **보관 개수 제한**: 실제로 게시할 때는 현재 버전 기준 **최근 3개 버전까지만** `releases/`에 남기고 그보다 오래된 버전 파일은 저장소에서 삭제할 것(사용자 요청: "용량문제는 현재 버전 기준 이전 3개만 남기고 저장소에서 삭제해"). 단, `git rm`으로 지워도 과거 커밋의 git 히스토리 자체에는 이미 남아 있어 클론 용량이 소급 감소하진 않음(히스토리 재작성 없이는) - 이건 "앞으로의 작업 트리를 가볍게 유지"하는 것이지 과거 커밋들의 blob을 지우는 게 아니라는 점을 사용자에게 필요시 설명할 것.
  - `mcp_servers/`(MediaWiki MCP 서버 실행 파일)는 이 규칙과 무관 - 그건 가끔만 바뀌므로 사용자 승인 하에 이미 커밋됨.
